jQuery(function () {
  var Slider = function (selector) {
    this._slider = jQuery(selector);
    this._container = this._slider.find('.container');
    this._slides = this._container.find('.slides');
  }
  Slider.prototype = {

  }
  new Slider('.slides');
});
